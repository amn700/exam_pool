# A-Maze-ing default configuration file
# Lines starting with '#' are comments and are ignored by the parser.
# Format: KEY=VALUE (one pair per line, no spaces around '=')

WIDTH=20
HEIGHT=15
ENTRY=0,0
EXIT=19,14
OUTPUT_FILE=maze.txt
PERFECT=True

# Optional keys
SEED=42
